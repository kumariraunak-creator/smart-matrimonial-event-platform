{
  "name": "smart-matrimonial-event-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend && node -e \"const fs=require('fs'); fs.cpSync('frontend/dist', './dist', {recursive:true}); fs.copyFileSync('frontend/dist/index.html', './index.html')\"",
    "vercel-build": "npm run build"
  }
}
